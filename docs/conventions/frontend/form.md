除表格搜索列表外，其他所有表单场景均须使用 Element Plus 的 `el-form` 组件。

#### **配置要求**

- **组件选择**：必须使用 `el-form` 组件。
- **标题对齐**：表单标题需设置为左对齐。
- **标签位置 (`label-position`)**：必须设置为 `left`。
- **标签宽度 (`label-width`)**：必须设置为 `auto`。

